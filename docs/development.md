# Руководство по разработке

Документ описывает процессы разработки, рабочий процесс, команды и лучшие практики для работы с проектом.

## 🚀 Запуск проекта

### Разработка

```bash
# Запуск сервера разработки с HMR (Hot Module Replacement)
pnpm dev
```

Приложение будет доступно по адресу: http://localhost:3333

**Что включает в себя `pnpm dev`:**
- Запуск AdonisJS сервера
- Запуск Vite dev сервера с HMR для React компонентов
- Автоматическая перезагрузка при изменении файлов
- Hot Hook для контроллеров и middleware (автоматическая перезагрузка без перезапуска сервера)

### Сборка для продакшена

```bash
# Сборка приложения
pnpm build

# Запуск продакшен сервера
pnpm start

# Preview продакшен сборки
pnpm preview
```

## 📝 Команды разработки

### Код

```bash
# Линтинг кода
pnpm lint

# Исправление ошибок линтинга автоматически
pnpm lint:fix

# Форматирование кода
pnpm format

# Проверка типов TypeScript
pnpm typecheck

# Все проверки (lint + typecheck)
pnpm check-all
```

### База данных

```bash
# Создать новую миграцию
node ace make:migration migration_name

# Выполнить миграции
node ace migration:run

# Откатить последнюю миграцию
node ace migration:rollback

# Откатить все миграции
node ace migration:rollback --batch=0

# Показать статус миграций
node ace migration:status

# Создать seeder
node ace make:seeder SeederName

# Запустить seeders
node ace db:seed
```

### Генерация кода

```bash
# Создать контроллер
node ace make:controller ControllerName

# Создать модель
node ace make:model ModelName

# Создать middleware
node ace make:middleware MiddlewareName

# Создать validator
node ace make:validator ValidatorName

# Создать service
node ace make:service ServiceName
```

### Документация

```bash
# Запуск VitePress документации в режиме разработки
pnpm docs:dev

# Сборка документации
pnpm docs:build

# Preview собранной документации
pnpm docs:preview
```

## 🔄 Рабочий процесс (Workflow)

### Добавление новой функции

1. **Создать ветку:**
   ```bash
   git checkout -b feature/my-feature
   ```

2. **Backend разработка:**
   - Создать контроллер: `node ace make:controller FeatureController`
   - Добавить маршруты в `start/routes.ts`
   - Создать валидаторы если нужно: `node ace make:validator FeatureValidator`
   - Обновить модели если нужно

3. **База данных:**
   - Создать миграцию: `node ace make:migration add_feature_table`
   - Выполнить миграцию: `node ace migration:run`

4. **Frontend разработка:**
   - Создать страницу в `resources/js/pages/`
   - Создать компоненты в `resources/js/components/` если нужно
   - Добавить типы в `resources/js/types/` если нужно

5. **Проверка кода:**
   - Проверить линтинг: `pnpm lint`
   - Проверить типы: `pnpm typecheck`

6. **Коммит и Push:**
   ```bash
   git add .
   git commit -m "feat: добавлена новая функция"
   git push origin feature/my-feature
   ```

### Commit сообщения

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - Новая функция
- `fix:` - Исправление бага
- `docs:` - Документация
- `style:` - Форматирование кода (не влияет на логику)
- `refactor:` - Рефакторинг кода
- `perf:` - Улучшение производительности
- `chore:` - Обновление зависимостей, конфигурации и т.д.
- `ci:` - Изменения CI/CD

**Примеры:**
```bash
git commit -m "feat: добавлена смена пароля"
git commit -m "fix: исправлена валидация email"
git commit -m "docs: обновлена документация по разработке"
```

## 🏗 Архитектура разработки

### Backend (AdonisJS)

**Структура:**
```
app/
├── controllers/     # HTTP контроллеры
├── models/          # Lucid ORM модели
├── middleware/      # Middleware для обработки запросов
├── validators/      # Валидаторы VineJS
├── services/        # Бизнес-логика
└── exceptions/      # Кастомные исключения

config/              # Конфигурационные файлы
database/
├── migrations/      # Миграции БД
└── seeders/         # Seeders для тестовых данных

start/
├── routes.ts        # Маршруты приложения
└── kernel.ts        # Регистрация middleware
```

**Паттерны:**
- Контроллеры обрабатывают HTTP запросы
- Валидаторы проверяют данные
- Модели работают с БД через Lucid ORM
- Сервисы содержат бизнес-логику

### Frontend (React + Inertia)

**Структура:**
```
resources/js/
├── app/             # Настройка приложения
│   ├── app.tsx      # Точка входа Inertia
│   └── app-wrapper.tsx  # Обертки (провайдеры)
├── pages/           # Inertia страницы
├── components/      # React компоненты
│   ├── ui/          # ShadCN UI компоненты
│   ├── layout/      # Layout компоненты
│   └── common/      # Общие компоненты
├── features/        # Feature-based компоненты
├── hooks/           # Custom React hooks
├── lib/             # Утилиты и константы
└── types/           # TypeScript типы
```

**Паттерны:**
- Pages - это Inertia страницы (компоненты страниц)
- Components - переиспользуемые компоненты
- Features - компоненты, сгруппированные по функциональности
- Hooks - переиспользуемая логика

## 🔧 Hot Reloading

### Backend Hot Reload

**Hot Hook** настроен для автоматической перезагрузки:
- Контроллеры (`app/controllers/**/*.ts`)
- Middleware (`app/middleware/*.ts`)

Изменения в этих файлах применяются без перезапуска сервера.

### Frontend Hot Reload

**Vite HMR** обеспечивает:
- Мгновенное обновление React компонентов
- Сохранение состояния компонентов
- Быструю перезагрузку стилей

## 📦 Зависимости

### Управление зависимостями

```bash
# Добавить зависимость
pnpm add package-name

# Добавить dev зависимость
pnpm add -D package-name

# Удалить зависимость
pnpm remove package-name

# Обновить зависимости
pnpm update

# Проверить устаревшие пакеты
pnpm outdated
```

### Основные зависимости

**Backend:**
- `@adonisjs/core` - Ядро AdonisJS
- `@adonisjs/lucid` - ORM для работы с БД
- `@adonisjs/auth` - Аутентификация
- `@adonisjs/inertia` - Интеграция с Inertia.js
- `@vinejs/vine` - Валидация

**Frontend:**
- `react` + `react-dom` - React библиотека
- `@inertiajs/react` - Inertia.js адаптер для React
- `tailwindcss` - CSS фреймворк
- `sonner` - Toast уведомления
- `lucide-react` - Иконки

## 🗄 Работа с базой данных

### Миграции

```typescript
// Создание миграции
node ace make:migration create_posts_table

// Пример миграции
export default class extends BaseSchema {
  protected tableName = 'posts'

  async up() {
    this.schema.createTable(this.tableName, (table) => {
      table.increments('id')
      table.string('title')
      table.text('content')
      table.timestamp('created_at')
      table.timestamp('updated_at')
    })
  }

  async down() {
    this.schema.dropTable(this.tableName)
  }
}
```

### Модели

```typescript
// Создание модели
import { BaseModel, column } from '@adonisjs/lucid/orm'

export default class Post extends BaseModel {
  @column({ isPrimary: true })
  declare id: number

  @column()
  declare title: string

  @column()
  declare content: string

  @column.dateTime({ autoCreate: true })
  declare createdAt: DateTime

  @column.dateTime({ autoCreate: true, autoUpdate: true })
  declare updatedAt: DateTime
}
```

### Использование моделей

```typescript
// Создание
const post = await Post.create({
  title: 'My Post',
  content: 'Content here'
})

// Поиск
const post = await Post.find(1)
const posts = await Post.all()

// Обновление
post.title = 'New Title'
await post.save()

// Удаление
await post.delete()
```

## 🔐 Аутентификация

### Работа с аутентификацией

```typescript
// В контроллере
export default class MyController {
  async index({ auth }: HttpContext) {
    // Проверка аутентификации
    await auth.use('web').authenticate()
    
    // Получение пользователя
    const user = auth.user!
    
    // ...
  }
}
```

### Middleware для защиты маршрутов

```typescript
// В start/routes.ts
router
  .group(() => {
    router.get('/protected', [MyController, 'index'])
  })
  .use([middleware.auth()])
```

## 🎨 Стилизация

### Tailwind CSS

Проект использует Tailwind CSS 4 с CSS переменными для тем.

```tsx
// Использование классов
<div className="bg-background text-foreground p-4">
  Content
</div>

// Темная тема
<div className="dark:bg-slate-800">
  Dark theme content
</div>
```

### CSS переменные

Темы определяются через CSS переменные в `resources/css/app.css`:

```css
:root {
  --background: oklch(1 0 0);
  --foreground: oklch(0.145 0 0);
  /* ... */
}

.dark {
  --background: oklch(0.145 0 0);
  --foreground: oklch(0.985 0 0);
  /* ... */
}
```

## 🐛 Отладка

### Логирование

```typescript
import logger from '@adonisjs/core/services/logger'

// Разные уровни логирования
logger.info('Info message')
logger.warn('Warning message')
logger.error('Error message')
logger.debug('Debug message')
```

### Отладка в браузере

- React DevTools для отладки React компонентов
- Redux DevTools (если используется)
- Network tab для просмотра запросов

### Отладка на сервере

```bash
# Запуск с детальными логами
NODE_ENV=development pnpm dev

# Просмотр логов
tail -f logs/app.log
```

## 📚 Полезные ресурсы

### Документация

- [AdonisJS Documentation](https://docs.adonisjs.com/)
- [Inertia.js Documentation](https://inertiajs.com/)
- [React Documentation](https://react.dev/)
- [Tailwind CSS Documentation](https://tailwindcss.com/)
- [ShadCN UI](https://ui.shadcn.com/)

### Инструменты

- **VS Code Extensions:**
  - ESLint
  - Prettier
  - Tailwind CSS IntelliSense
  - TypeScript и JavaScript Language Features

## ✅ Чеклист перед коммитом

- [ ] Код отлинтирован (`pnpm lint`)
- [ ] Типы проверены (`pnpm typecheck`)
- [ ] Код отформатирован (`pnpm format`)
- [ ] Миграции применены (`node ace migration:run`)
- [ ] Документация обновлена (если нужно)
- [ ] Commit message следует Conventional Commits

## 🎯 Лучшие практики

1. **Всегда используйте типы TypeScript** - избегайте `any`
2. **Валидируйте данные** - используйте VineJS валидаторы
3. **Следуйте структуре проекта** - не создавайте файлы в неправильных местах
5. **Используйте компоненты** - не дублируйте код
6. **Комментируйте сложную логику** - но не очевидные вещи
7. **Используйте meaningful имена** - переменные и функции должны быть понятными
8. **Держите функции небольшими** - один функция = одна ответственность
9. **Обрабатывайте ошибки** - всегда обрабатывайте возможные ошибки
10. **Документируйте изменения** - обновляйте README и документацию

## 🚨 Частые проблемы и решения

### Проблема: Миграции не применяются

**Решение:**
```bash
# Проверить статус миграций
node ace migration:status

# Откатить и применить заново
node ace migration:rollback
node ace migration:run
```

### Проблема: HMR не работает

**Решение:**
- Проверить, что Vite dev server запущен
- Перезапустить `pnpm dev`
- Очистить кеш: удалить `node_modules/.vite`

### Проблема: Типы не работают

**Решение:**
```bash
# Пересобрать типы
pnpm typecheck

# Перезапустить TypeScript server в VS Code
# Cmd/Ctrl + Shift + P -> "TypeScript: Restart TS Server"
```

### Проблема: Стили не применяются

**Решение:**
- Проверить, что Tailwind правильно настроен
- Проверить импорт стилей в `app.tsx`
- Очистить кеш браузера

## 📝 Дополнительные команды

```bash
# Очистить node_modules и переустановить
rm -rf node_modules pnpm-lock.yaml
pnpm install

# Проверить размер bundle
pnpm build
# Результат будет в dist/

# Анализ зависимостей
pnpm why package-name
```

## 📋 План разработки

### Страница управления пользователями (Users Management)

**Статус:** Планируется

**Описание:**
Страница для просмотра и управления пользователями системы. Доступна только администраторам.

**Требования:**
- Страница должна быть доступна только пользователям с ролью `admin`
- Отображение списка всех пользователей с информацией:
  - Имя (Full Name)
  - Email
  - Роль (Role)
  - Статус (Status: active/inactive)
  - Дата создания
- Возможности фильтрации и поиска
- Пагинация для большого количества пользователей

**Маршрут:**
- `/users` или `/admin/users` - список пользователей
- Требуется middleware для проверки роли администратора

**Backend:**
- Создать контроллер `UsersController` с методом `index` для получения списка пользователей
- Добавить middleware для проверки роли администратора
- Добавить роуты в `start/routes.ts`

**Frontend:**
- Создать страницу `resources/js/pages/users/index.tsx`
- Создать компоненты для таблицы пользователей
- Добавить фильтрацию и поиск по имени или email
- Добавить кнопку создания нового пользователя
- Добавить кнопку редактирования для каждой строки таблицы
- Добавить пагинацию

**База данных:**
- Использовать существующую модель `User`
- Возможные миграции для индексов, если нужно оптимизировать поиск

**Приоритет:** Средний

---

**Примечание:** Этот документ постоянно обновляется. Если вы заметили устаревшую информацию или хотите добавить что-то новое, создайте Pull Request!

